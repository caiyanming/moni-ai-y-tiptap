# 默认使用系统mirror安装依赖
registry=https://registry.npmmirror.com/

# 私有包发布到本地 Verdaccio，强制携带认证
@tiptap:registry=https://registry-zonbov-5xySne-raqbot.fufenxi.com/
@tiptap:always-auth=true

# 发布认证请使用用户级 ~/.npmrc，避免在仓库中暴露token
